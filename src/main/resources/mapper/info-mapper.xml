<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="info">
    <select id="list" resultType="info">
        SELECT *
        FROM examination.info
        ORDER BY id
    </select>
    <select id="search" resultType="info">
        SELECT *
        FROM examination.info
        WHERE id = #{id}
    </select>
    <update id="modify">
        UPDATE examination.info
        SET
            date = #{date}, 
            baoxian = #{baoxian}, 
            gongjijin = #{gongjijin}, 
            userId = #{userId}
        WHERE id = #{id}
    </update>
    <delete id="remove">
        DELETE FROM examination.info
        WHERE id = #{id}
    </delete>
    <insert id="create" parameterType="info" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO examination.info (
            date, 
            baoxian, 
            gongjijin, 
            userId
        )
        VALUES (
            #{date}, 
            #{baoxian}, 
            #{gongjijin}, 
            #{userId}
        );
    </insert>
</mapper>