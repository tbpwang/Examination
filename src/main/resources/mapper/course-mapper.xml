<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="course">
    <select id="list" resultType="course">
        SELECT *
        FROM examination.course
        ORDER BY id
    </select>
    <select id="search" resultType="course">
        SELECT *
        FROM examination.course
        WHERE id = #{id}
    </select>
    <update id="modify">
        UPDATE examination.course
        SET
            title   = #{title},
            time    = #{time},
            teacher = #{teacher},
            score   = #{score}
        WHERE id = #{id}
    </update>
    <delete id="remove">
        DELETE FROM examination.course
        WHERE id = #{id}
    </delete>
    <insert id="create" parameterType="course" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO examination.course (
            title,
            time,
            teacher,
            score
        )
        VALUES (
            #{title},
            #{time},
            #{teacher},
            #{score}
        );
    </insert>

    <resultMap id="course_students_map" type="course">
        <result property="title" column="title"/>
        <collection property="students" ofType="user">
            <result property="username" column="username"/>
        </collection>
    </resultMap>
    <select id="course_students" resultMap="course_students_map">
        SELECT
            c.title,
            u.username
        FROM examination.user u
            JOIN examination.course c
            JOIN examination.student_course s
                ON u.id = s.studentId AND c.id = s.courseId;
    </select>
</mapper>